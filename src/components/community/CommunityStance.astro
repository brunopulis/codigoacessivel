---
const rejectedExcuses = [
  '"vamos arrumar só para o cliente assinar"',
  '"ninguém usa leitor de tela mesmo"',
  '"depois a gente vê isso"',
];
---

<section class="px-6 py-20 lg:py-32 bg-card">
  <div class="max-w-4xl mx-auto text-center">
    <h2 class="text-2xl md:text-4xl font-bold mb-8">
      Já perdi oportunidades por não aceitar:
    </h2>
        
    <div class="space-y-4 mb-12 max-w-2xl mx-auto">
      {rejectedExcuses.map((excuse) => (
        <p 
          class="text-lg md:text-xl text-muted-foreground italic p-4 bg-secondary/20 rounded-lg border border-border"
        >
          {excuse}
        </p>
      ))}
    </div>
        
    <p class="text-2xl md:text-3xl font-bold text-primary">
      E perder essas oportunidades valeu a pena.
    </p>
  </div>
</section>
  