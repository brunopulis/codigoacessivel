---
import { Check, X } from "@lucide/astro";

const forYou = [
  "querem parar de fingir que fazem inclusão",
  "aceitam rever decisões técnicas",
  "entendem que qualidade não é opcional",
];

const notForYou = [
  "quer um selo",
  "quer um checklist rápido",
  "quer uma desculpa técnica",
]
---

<section class="px-6 py-20 lg:py-32">
  <div class="max-w-5xl mx-auto">
    <h2 class="text-2xl md:text-4xl font-bold text-center mb-12">
      Essa comunidade é para desenvolvedores e times que:
    </h2>

    <div class="grid md:grid-cols-2 gap-8 mb-16">
      {/* Bloco "É para você" */}
      <div class="p-8 rounded-xl bg-card border border-primary/30">
        <h3 class="text-xl font-bold text-primary mb-6 flex items-center gap-2">
          <Check class="w-6 h-6" aria-hidden="true" />
          É para você se
        </h3>
        <ul class="space-y-4">
          {forYou.map((item) => (
            <li class="flex items-start gap-3">
              <Check class="w-5 h-5 text-primary shrink-0 mt-0.5" aria-hidden="true" />
              <span class="text-foreground">{item}</span>
            </li>
          ))}
        </ul>
      </div>

      {/* Bloco "Não é para você" */}
      <div class="p-8 rounded-xl bg-card border border-destructive/30">
        <h3 class="text-xl font-bold text-destructive mb-6 flex items-center gap-2">
          <X class="w-6 h-6" aria-hidden="true" />
          Não é para você se
        </h3>
        <ul class="space-y-4">
          {notForYou.map((item) => (
            <li class="flex items-start gap-3">
              <X class="w-5 h-5 text-destructive shrink-0 mt-0.5" aria-hidden="true" />
              <span class="text-muted-foreground">{item}</span>
            </li>
          ))}
        </ul>
      </div>
    </div>

    <div class="text-center max-w-3xl mx-auto">
      <p class="text-xl md:text-2xl text-muted-foreground mb-4">
        Se você quer um selo, um checklist rápido ou uma desculpa técnica,{" "}
        <span class="text-foreground">provavelmente não é aqui.</span>
      </p>
      <p class="text-xl md:text-2xl text-foreground font-medium">
        Mas se você quer{" "}
        <strong class="text-primary">construir software que respeita gente</strong>,{" "}
        então a conversa começa aqui.
      </p>
    </div>
  </div>
</section>